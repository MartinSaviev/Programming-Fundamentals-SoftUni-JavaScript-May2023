function matrix(num) {

    for (let i = 0; i < num; i++) {
        let array = [];
        for (let i = 0; i < num; i++) {
            array.push(num)
        }
        console.log(array.join(' '));   
    }
   
}